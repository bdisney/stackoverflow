$('.toast').remove();
<% if @answer.errors.any? %>
  $('#flash-errors').html('<%= j render 'shared/errors_messages', resource: @answer %>');
<% else %>
  $('#answer-<%= @answer.id %>').html('<%= j render @answer %>');
  toastr.success('Answer was updated.');
<% end %>